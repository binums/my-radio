# Radio Calico Environment Configuration
# Copy this file to .env and update with your settings

# Application Settings
NODE_ENV=production
PORT=3000

# Database Configuration (Production)
DB_HOST=postgres
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=changeme-secure-password
DB_NAME=prototype_db

# Test Database Configuration (Development Only)
TEST_DB_HOST=postgres-test
TEST_DB_PORT=5432
TEST_DB_USER=admin
TEST_DB_PASSWORD=changeme-test-password
TEST_DB_NAME=prototype_db_test

# Docker-specific settings
# These are used by docker-compose files
# Uncomment and modify if needed

# COMPOSE_PROJECT_NAME=radiocalico
# COMPOSE_FILE=docker-compose.yml
